import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tarang.incois',
  appName: 'Tarang',
  webDir: 'www', // Or 'dist', or 'build' - whatever your web app builds to
};

export default config;
